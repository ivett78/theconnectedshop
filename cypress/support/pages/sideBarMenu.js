import HomePage from "./homePage";

class SideBarMenu {

//method Search Sidebar Menu Button
//    findHeaderIcon() {
 //       cy.get(':nth-child(1) > .Header__Icon')
  //          .should('be.visible');
    }
 //method Expand Sidebar Menu
 //   collapseSideBarMenu()
{
 /*   cy.get('button.Drawer__Close[data-action="close-drawer"][data-drawer-id="sidebar-menu"]').then($button => {
        if ($button.is(':visible')) {
            cy.wrap($button).click({force: true});
            cy.log('Sidebar menu button is visible');
        } else {
            cy.log('Sidebar menu button is not visible');
        }
    });*/
}
export default SideBarMenu;